# Dashboard

Launch the professional FE511 analytics dashboard with:

`streamlit run dashboard/app.py`

Recommended flow before demo:
1. `python run_analysis.py`
2. `streamlit run dashboard/app.py`

The app reads generated artifacts from `outputs/` and `docs/executive_summary.md`.
