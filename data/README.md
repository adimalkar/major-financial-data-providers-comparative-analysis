# Data Layout

- `data/raw/`: raw extracts or downloaded files.
- `data/interim/`: cleaned intermediate datasets.
- `data/processed/`: model-ready datasets for analytics modules.

Current project artifacts are in `FE511_Project/` and are used as the default input source by `run_analysis.py`.
